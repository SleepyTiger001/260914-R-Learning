# ============================================================
#  260914-R-Learning  .gitignore
#  原则：只提交「代码 + 文档」，可再生产物与本地痕迹一律排除
# ============================================================

# ---- R 语言产物 -------------------------------------------------
.Rhistory              # 交互式命令历史，含本地绝对路径
.Rapp.history
.RData                 # 工作空间镜像，换台机器就不可复现
.Ruserdata
Rplots.pdf             # 命令行画图的默认输出，每次运行都重新生成
*.Rproj
.Rproj.user/
.Renviron              # 本地环境变量，可能含令牌/密钥
.Rprofile              # 个人启动脚本，属本机配置
*.rds                  # R 原生二进制，不适合入库
*.RData

# ---- 脚本运行产生的可再生产物 -----------------------------------
# 跑一次 01_R_base_tidyverse_basics.R 即可全部重建
# 若希望把运行结果也发布出去，删掉下面两行即可（CSV/PNG 体积都很小）
data/
outputs/

# ---- macOS ------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ---- 编辑器 / IDE ----------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
~$*

# ---- 内部工作记录（不对外发布）----------------------------------
.workbuddy/

# ---- R 包开发相关（本仓库暂不需要，预留避免误提交）--------------
*.tar.gz
*.tgz
