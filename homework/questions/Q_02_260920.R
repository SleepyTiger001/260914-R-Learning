# =============================================================================
#  作业 02 · 用管道处理体检数据（MFF-TDF.xlsx）
# -----------------------------------------------------------------------------
#  课程：scripts/01_base_tidyverse/01_R_base_tidyverse_basics.R（只用到第 00–07 节）
#  数据：data/01_base_tidyverse/MFF-TDF.xlsx，工作表 selected_600
#  要求：每题写成一条用 |> 串起来的链，不要拆成 a <- ...; b <- ... 的多步赋值。
#  提交：答案存为 homework/solutions/<你的名字>/S_02_260920.R，步骤见 homework/HOWTO_SUBMIT.md
# =============================================================================

# ---- 题 1 ---------------------------------------------------------------
#   导入 tidyverse 和 rio 工具包。
#   用代码创建输出目录 homework/outputs/<你的名字>/。
#   把 MFF-TDF.xlsx 读为 mff 变量，后三题都以它为起点。

# ---- 题 2 ---------------------------------------------------------------
#   取任意 3 个数值列 → as.matrix() → colMeans() → round(2)，打印均值。

# ---- 题 3 ---------------------------------------------------------------
#   取「年龄、体重」两列 → 转为 matrix → 转为 data.frame → 转为 tibble → str()。

# ---- 题 4 ---------------------------------------------------------------
#   删除「血压」相关列 → 新增一列 bmi 计算 BMI → 筛选高于平均 BMI 的行，存进 mff_top。
#   再导出到 homework/outputs/<你的名字>/mff_02_top_bmi.csv
