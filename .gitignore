# ============================================================
#  260914-R-Learning  .gitignore
#  原则：提交「代码 + 文档 + 随仓库分发的数据」，其余本地产物一律排除
# ============================================================

# ---- R 语言产物 -------------------------------------------------
.Rhistory              # 交互式命令历史，含本地绝对路径
.Rapp.history
.RData                 # 工作空间镜像，换台机器就不可复现
.Ruserdata
Rplots.pdf             # 命令行画图的默认输出，每次运行都重新生成
# 注意：*.Rproj 是 RStudio 项目文件，它决定工作目录，必须随仓库提交，故不忽略
.Rproj.user/           # RStudio 个人会话状态（窗口布局、命令历史），属本机配置
.Renviron              # 本地环境变量，可能含令牌/密钥
.Rprofile              # 个人启动脚本，属本机配置
*.RData

# ---- 脚本运行产生的可再生产物 -----------------------------------
# data/ 随仓库分发：按「课次_主题」建子目录（如 data/01_base_tidyverse/），单文件控制在 100 KB 以内
# outputs/ 默认不入库，跑一次 scripts/NN_主题/ 下的脚本即可全部重建；
#          若希望把运行结果也发布出去，删掉下面一行即可（CSV/PNG 体积都很小）
#          注意前面有斜杠：只忽略项目根目录的 outputs/，homework/outputs/<你的名字>/ 随作业一起提交
/outputs/

# ---- 作业参考答案（不对外发布）----------------------------------
# 参考答案只在本机留存：学生交卷前不应看到，故不推送
homework/answers/
homework/outputs/ANSWERS

# ---- macOS ------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ---- 编辑器 / IDE ----------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
~$*

# ---- 内部工作记录（不对外发布）----------------------------------
.workbuddy/

# ---- R 包开发相关（本仓库暂不需要，预留避免误提交）--------------
*.tar.gz
*.tgz
