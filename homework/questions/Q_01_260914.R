# =============================================================================
#  作业 01：字符串日期 → 日期型 → 用 difftime() 计算两个日期的间隔
# -----------------------------------------------------------------------------
#  对应课程：scripts/01_base_tidyverse/01_R_base_tidyverse_basics.R
#            第 01 节（Date 类型与 as.Date）、第 07 节（写文件）
#  发布批次：260914            满分：100 分
#  提交方式：
#    1) 把你的答案写在 solutions/<你的名字>/ 目录下，文件名保持 Q_01_260914.R；
#    2) 文件首行写上「姓名 + 完成时间」；
#    3) 保存后提交到本仓库（git add / commit / push）。
#      —— 老师答案在 homework/answers/A_01_260914.R，交卷前请不要打开。
#  评分点：① 脚本能一次跑通（30 分）② 每行代码都有中文注释（20 分）
#          ③ difftime() 的五个单位都用上，结果带单位说明（30 分）
#          ④ 打印格式清晰、读得通（20 分）
# =============================================================================


# ---- 题目背景 ----------------------------------------------------------------
# 你认识某个人、或者开始做某件事，已经多久了？这个问题在 R 里就是"两个日期相减"。
# Base R 算日期间隔只用一个函数：difftime()。它的 units 参数决定"以什么单位说话"，
# 能选的单位就是下面这五个 —— 本次作业只练这个函数，不引入任何扩展包。


# ---- 具体要求 ----------------------------------------------------------------

# 要求 1（字符串 → 变量）
#   定义两个「字符串类型」的日期变量（注意：带引号，是字符不是日期）：
#       start_str = "2021-09-01"
#       end_str   = "2026-09-14"
#   并用 class() 打印它们的类型，确认拿到的是 character。

# 要求 2（字符串 → 日期型）
#   用 as.Date() 把上面两个字符串转成日期型，分别存进 start 与 end；
#   再用 class() 确认已经变成 "Date"，并 print() 看看它们长什么样。

# 要求 3（五个单位各算一遍）
#   用 difftime() 计算 end 与 start 的间隔，units 参数依次取：
#       "secs"（秒）、"mins"（分）、"hours"（小时）、"days"（天）、"weeks"（周）
#   每个结果都用 as.numeric() 取出纯数字再打印，五个数字都要出现在控制台上。

# 要求 4（整周与余天）
#   把天数换算成"整几周零几天"：
#       整周数用 %/%（整除），余下的天数用 %%（取余）。
#   打印时这两种都要给出来，例如：262 周零 5 天。

# 要求 5（打印成人话）
#   用 cat() 或 print() 把结果打印出来，格式参考：
#       2021-09-01 到 2026-09-14 相差 1839 天（262 周零 5 天），
#       合 44136 小时 / 2648160 分钟 / 158889600 秒，约 262.71 周
#   注意：cat() 直接打印日期对象会输出"距 1970-01-01 的天数"（比如 18871），
#   想打出 2021-09-01 这种样子，得先 as.character() 或 format() 转一下。

# 要求 6（进阶，交付）
#   把 秒 / 分 / 小时 / 天 / 周 五个结果装进一个 tibble 并打印；
#   再用 write_csv() 存成 outputs/01_base_tidyverse/tables/date_span.csv。


# ---- 提示 --------------------------------------------------------------------

# 提示 1｜difftime() 的签名：
#         difftime(time1, time2, tz, units = c("secs", "mins", "hours", "days", "weeks"))
#         units 只能取 auto / secs / mins / hours / days / weeks 这几个值，
#         写成 "months" 或 "years" 会直接报错 —— 日期间隔没有"月""年"这种单位。

# 提示 2｜参数顺序决定正负：difftime(end, start) 是正数，写反了会得到 -1839 天。
#         另外 difftime() 结果不是普通数字，它带 units 属性，as.numeric() 之后才好参与运算。

# 提示 3｜字符串转日期：as.Date("2021-09-01") 默认按 YYYY-MM-DD 解析；
#         源数据换成 "2021/09/01" 或 "09/01/2021"，必须显式给 format 参数：
#         as.Date("2021/09/01", format = "%Y/%m/%d")。

# 提示 4｜时间单位换算关系（自己验算答案对不对）：
#         1 天 = 24 小时 = 1440 分钟 = 86400 秒；1 周 = 7 天。

# 提示 5｜打印用 cat() 拼接比 print() 更像人话；控制小数位用 round(x, 2)。
#         模板字符串 str_glue("{start} 到 {end} 相差 {days} 天") 可读性最好。

# 提示 6｜写文件前先确认目录存在：fs::dir_create("outputs/01_base_tidyverse/tables")。


# ---- 请在下方作答（把代码写在这条线的下面，每行都要有中文注释）----------------

# 要求 1：定义两个字符串日期并检查类型

# 要求 2：转成日期型

# 要求 3：公式五个单位各算一遍（secs / mins / hours / days / weeks）

# 要求 4：整周数与余天数

# 要求 5：打印成人话

# 要求 6（进阶）：tibble + 写出 CSV
